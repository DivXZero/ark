# config/app.yml for rails-settings-cached
defaults: &defaults
  config:
    repo:
      url: ''
      watch: false
    app:
      autoUpdate: true

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
